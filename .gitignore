.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# Reference-only copies of pre-refactor code, kept outside Sources/
# while the render/graphics stack is being split into sibling packages.
/RenderEngine/
build/
Package.resolved

# Compiled Python extension modules produced by the Swift build.
src/nucleant/*.so
src/nucleant/*.dylib
